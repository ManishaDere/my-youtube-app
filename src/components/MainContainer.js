import React from "react";
import VideoContainer from "./VideoContainer";

const MainContainer = () => {
  return (
    <div className="p-2">
      <VideoContainer />
    </div>
  );
};

export default MainContainer;
